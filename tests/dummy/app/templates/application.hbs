<h2 id='title'>Paper Password</h2>

{{paper-password
	value=password
	label='password'
	class='layout-fill flex-50'
	onChange=(action (mut password))
}}

<br/><br/>

{{paper-password
	reveal=false
	value=password
	label='password'
	class='layout-fill flex-50'
	onChange=(action (mut password))
}}

<br/><br/>

{{paper-password
	reveal=true
	value=password
	label='password'
	showPasswordStrength=false
	class='layout-fill flex-50'
	onChange=(action (mut password))
}}
